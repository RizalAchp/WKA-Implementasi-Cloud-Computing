import { Box, Card, Container } from "@mantine/core";
import { CountryCards } from "./cards-comp";

export default function Preferences() {
  return (
    <Container>
      <CountryCards />
    </Container>
  );
}
